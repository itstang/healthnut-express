## Getting Started - WIP

Clone repo:
```
$ git clone git@github.com:itstang/healthnut-express.git
$ cd healthnut-express
```

### Install Yarn
With homebrew:
`$ brew install yarn`

With npm
`$ npm install -g yarn`

Install dependencies
`$ yarn`

Start server
`$ yarn start`

Run tests
`$ yarn test`
